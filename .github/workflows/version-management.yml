name: Automatic Version Management

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: write
  actions: read
  checks: read
  pull-requests: write

jobs:
  version-management:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Install semantic-release dependencies
      run: |
        npm install --no-save \
          semantic-release \
          @semantic-release/changelog \
          @semantic-release/git \
          @semantic-release/github \
          @semantic-release/exec

    - name: Update VERSION file and create release
      continue-on-error: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        # Create semantic-release config
        cat > .releaserc.json << 'EOF'
        {
          "branches": ["main"],
          "plugins": [
            "@semantic-release/commit-analyzer",
            "@semantic-release/release-notes-generator",
            [
              "@semantic-release/exec",
              {
                "prepareCmd": "echo '{\"version\": \"v${nextRelease.version}\", \"commit\": \"'$GITHUB_SHA'\", \"build_date\": \"'$(date -u +%Y-%m-%dT%H:%M:%SZ)'\", \"build_type\": \"production\"}' > VERSION"
              }
            ],
            [
              "@semantic-release/git",
              {
                "assets": ["VERSION"],
                "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
              }
            ],
            "@semantic-release/github"
          ]
        }
        EOF
        
        # Run semantic-release
        npx semantic-release

    - name: Deploy to template repository
      if: always()
      env:
        GITHUB_TOKEN: ${{ secrets.PAT_TOKEN || secrets.PERSONAL_ACCESS_TOKEN || secrets.GH_TOKEN || secrets.GITHUB_TOKEN }}
      run: |
        # Clone the template repository with authentication
        git clone https://x-access-token:$GITHUB_TOKEN@github.com/vanman2024/multi-agent-claude-code.git template-repo
        cd template-repo
        
        # Configure git
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        
        # Create a new branch for this deployment
        BRANCH_NAME="auto-deploy/devops-$(date +%Y%m%d-%H%M%S)"
        git checkout -b $BRANCH_NAME
        
        # Use intelligent sync script to copy ALL application files (excluding dev/test)
        echo "🚀 Using intelligent auto-deploy sync..."
        chmod +x ../scripts/intelligent-auto-deploy.sh
        ../scripts/intelligent-auto-deploy.sh . ..
        
        # Check if there are any changes
        if git diff --quiet && git diff --cached --quiet; then
          echo "No changes to deploy"
          exit 0
        fi
        
        # Get version safely
        VERSION=$(jq -r '.version' ../VERSION 2>/dev/null || echo "unknown")
        echo "Deploying version: $VERSION"
        
        # Commit and push changes
        git add -A
        git commit -m "feat: Auto-deploy DevOps components $VERSION

        Auto-deployment from vanman2024/devops
        Commit: $GITHUB_SHA
        Build date: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
        
        git push origin $BRANCH_NAME
        
        # Create pull request using GitHub CLI
        PR_URL=$(gh pr create \
          --title "Auto-deploy DevOps $VERSION" \
          --body "Automated deployment of DevOps components from vanman2024/devops
        
        ## Changes
        - Updated DevOps automation tools
        - Version: $VERSION
        - Source commit: $GITHUB_SHA
        
        This PR was automatically created by the DevOps repository's semantic versioning workflow." \
          --base main \
          --head $BRANCH_NAME)
        
        # Auto-merge the PR
        echo "🔄 Attempting to auto-merge PR: $PR_URL"
        if gh pr merge $PR_URL --squash --delete-branch; then
          echo "✅ Auto-merged PR: $PR_URL"
        else
          echo "❌ Auto-merge failed, PR created but requires manual merge: $PR_URL"
          exit 1
        fi

